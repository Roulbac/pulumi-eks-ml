config:
  # Required by the AWS provider to know where to create resources.
  aws:region: us-west-2
  node_pools:
    - capacity_type: on-demand
      instance_type: t3.large
      name: general
    - capacity_type: on-demand
      instance_type: g5.2xlarge
      name: gpu-light

name: starter
description: A minimal starter project with a single EKS cluster in one region
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
  node_pools:
    type: array
    description: Karpenter node pool definitions (instance_type list, optional instance_family/instance_category lists).
    items:
      type: object